<%= form_with(model: task, local: true) do |f| %>

<%= render 'error_messages', task: @task %>

<%= f.label :status, 'ステータス' %>
  <%= f.text_field :status %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>